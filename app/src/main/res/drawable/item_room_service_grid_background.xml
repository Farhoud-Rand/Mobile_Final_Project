<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Replace @color/white with your desired background color -->
    <corners android:radius="8dp" /> <!-- Adjust the radius value to control the roundness of the edges -->
    <stroke
        android:width="4dp"
    android:color="@color/black" />
</shape>
