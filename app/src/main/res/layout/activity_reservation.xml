<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:foregroundTint="#FFFBFB"
    android:orientation="vertical"
    android:padding="20dp">

    <Spinner
        android:id="@+id/durationSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/durations"
        android:padding="10dp"
        android:prompt="@string/choose_duration_prompt" />

    <com.example.final_project.Models.CustomCalendarView
        android:id="@+id/calendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#FFFCFC"
        android:calendarTextColor="#FF0000"
        android:yearListSelectorColor="@color/purple_500" />

    <Button
        android:id="@+id/servicesButton"
        android:layout_width="149dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/go_to_services" />

    <Space
        android:layout_width="match_parent"
        android:layout_height="140dp" />

    <Button
        android:id="@+id/reserveButton"
        android:layout_width="199dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="Reserve" />

</LinearLayout>